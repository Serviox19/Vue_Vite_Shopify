{
  "name": "shopify-vue-theme",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "shopify:dev": "shopify theme dev --path=shopify",
    "shopify:push": "shopify theme push --path=shopify",
    "shopify:pull": "shopify theme pull --path=shopify",
    "deploy": "npm run build && shopify theme push --path=shopify"
  },
  "dependencies": {
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "tailwindcss": "^4.0.0",
    "vite": "^5.0.0",
    "vite-plugin-shopify": "^3.0.0"
  }
}
