# Shopify CLI configuration
# Built theme files are in ./dist directory

[theme]
root = "dist"

# Configure your store for development
[environments.development]
store = "merceitt-development.myshopify.com"
